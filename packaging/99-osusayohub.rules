# Allow the desktop user to read raw input events (KPS/UR tracking)
# from the keypad without root. Update idVendor/idProduct to match
# your device (`lsusb`).
KERNEL=="event*", SUBSYSTEM=="input", ATTRS{idVendor}=="8089", ATTRS{idProduct}=="0009", TAG+="uaccess"
